[pytest]
minversion = 8.0
addopts = -ra
testpaths = tests
markers =
    unit: fast isolated tests for pure modules and mapping logic
    smoke: local HTTP-level tests for endpoint behavior
    integration: live tests against Whoop API v2 (manual gated)
asyncio_mode = auto
